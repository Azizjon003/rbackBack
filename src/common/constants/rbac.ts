export enum Action {
  READ = 'READ',
  WRITE = 'WRITE',
  DELETE = 'DELETE',
}

export enum Resource {
  USERS = 'USERS',
  ROLES = 'ROLES',
}

export enum Role {
  ADMIN = 'ADMIN',
  USER = 'USER',
  MODERATOR = 'MODERATOR',
}
